import { useState } from 'react'

function Nav() {
  // build a nav bar for the site
  // which has a drop  down for items, a cart and a user profile, home page and about page
  const [hoveredButton, setHoveredButton] = useState('')

  return (
    <>
      <Nav>
        
      </Nav>
    </>
  )
}

export default Nav
